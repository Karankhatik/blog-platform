import {footerLinks} from '@/data/footerLinks';
import { socialLinks } from '@/data/socialLink';
import {navbarItems }from '@/data/navbarData';
import { themeData } from '@/data/themeData';
import {features} from './featureSectionData';
export{
    footerLinks,
    socialLinks,
    navbarItems,
    themeData,
    features
}